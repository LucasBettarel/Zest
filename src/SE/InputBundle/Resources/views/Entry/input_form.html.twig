{% extends "SEInputBundle:Entry:input.html.twig" %}

{% macro entryCollection(entry) %}
  <tr>
    <td>time_name</td>
    <td class="table-size">
      <div class="float-left input-employee">
        {{ form_widget(entry.employee, {'attr': {'class': 'form-control'}}) }}
      </div>
      <div class="float-left input-sesa">
        {{ form_widget(entry.sesa, {'attr': {'class': 'form-control'}}) }}
      </div>
      <div id="presence">
        <div class="float-left" data-toggle="tooltip" title="Presence"> 
          {{ form_widget(entry.present, {'attr': {'class': 'input-present '}}) }}
        </div>
        <div class="hide toggling">
          {{ form_widget(entry.absence_reason, {'attr': {'class': 'form-control input-reason'}}) }}
        </div>
      </div>
    </td>
    <td id="activities">
      <div 
        id="activities-prototype"
        data-prototype="{{ _self.activityCollection(entry.activity_hours.vars.prototype) | e }}">
          {% for activity in entry.activity_hours %}
            {{ _self.activityCollection(activity) }}
          {% endfor %}
      </div>
    </td>
    <td>
      <a href=# id="comment" class="btn btn-primary">
        <i class="glyphicon glyphicon-comment"></i>
      </a>
      <a href=# id="rmv" class="btn btn-danger" data-target="entries-prototype">
        <i class="glyphicon glyphicon-trash"></i>
      </a>
      <a href=# id="info" class="btn btn-info">
        <i class="glyphicon glyphicon-info-sign"></i>
      </a>
      <a href=# id="add" class="btn btn-success" data-target="activities-prototype">
        <i class="glyphicon glyphicon-plus"></i>
      </a>
      <div class="hide toggling txtarea-sm">
        {{ form_widget(entry.comments, {'attr': {'class': 'form-control'}}) }}
      </div>  
    </td>
    {{ form_widget(entry) }}
  </tr>
{% endmacro %}

{% macro activityCollection(activity) %}
  <div class="row">
    <div class="input-activity float-left">{{ form_widget(activity.activity, {'attr': {'class': 'form-control'}}) }} </div>
    <div class="float-left">
      <div class="float-left input-regular-hours">
        {{ form_widget(activity.regularHours, {'attr': {'class': 'form-control'}}) }}
      </div>
      <div class="float-left input-overtime" title="Enter overtime timings">
        {{ form_widget(activity.otHours, {'attr': {'class': 'form-control', 'readonly':""}}) }}
      </div>
      <div class='float-left toggling input-ot-time hide'>
        <div class="float-left">
          <div data-toggle="tooltip" title="Start time">
            {{ form_widget(activity.otStartTime, {'attr': {'class': 'form-control'}}) }}
          </div>
        </div>
        <div>
          <div data-toggle="tooltip" title="End time">
            {{ form_widget(activity.otEndTime, {'attr': {'class': 'form-control'}}) }}
          </div>
        </div>
      </div>
    </div>
    <div class="float-left" data-toggle="tooltip" title="Transfer out">
      <input class="transfer" type="checkbox" value="0">
    </div>
    <div class="float-left input-zone">
      {{ form_widget(activity.zone, {'attr': {'class': 'form-control'}}) }}
    </div>
    <div>
      <a href=# id="rmv" class="text-danger input-rmv" data-target="activities-prototype">
        <i class="glyphicon glyphicon-remove"></i>
      </a>
    </div>   
    {{ form_widget(activity) }}
  </div>
  {% endmacro %}

 {% block seinput_form %}

  <div class="well">
   {{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}

    {{ form_errors(form) }}

     <div class="form-group">
        {{ form_errors(form.user) }}
        <div class="col-md-3">
          <div class="input-group">
            <span class="input-group-addon">
              <span id="user" class="glyphicon glyphicon-user">
              </span>
            </span>
            {{ form_widget(form.user, {'attr': {'class': 'form-control', 'placeholder' : 'Please insert your name'}}) }}
          </div>
        </div>
        <div class="col-md-2">
          <div class="input-group">
          <span class="input-group-addon">
              <span id="team" class="glyphicon glyphicon-retweet">
              </span>
            </span>
            {{ form_widget(form.team, {'attr': {'class': 'input-team form-control'}}) }}
          </div>
        </div>
        <div class="col-md-2">
          <div class="input-group">
            <span class="input-group-addon">
              <span id="shift" class="glyphicon glyphicon-time">
              </span>
            </span>
            {{ form_widget(form.shift, {'attr': {'class': 'form-control input-shift'}}) }}
          </div>
        </div>
        <div class="col-md-2">
          <div class="input-group">
            <span class="input-group-addon">
              <span id="date-input" class="glyphicon glyphicon-calendar">
              </span>
            </span>
            {{ form_widget(form.dateInput, {'attr': {'class': 'form-control input-date'}}) }}
          </div>
        </div>
        <div class="pull-right">
          <a href=# id="add" class="btn btn-success" data-target="entries-prototype">
            <i class="glyphicon glyphicon-plus"></i> Add Employee
          </a>
        </div> 
    </div>
    
    <div class="form-group">
     
     <div class="table-responsive">
        <table class="table table-hover table-striped">
          <thead>
            <tr>
              <th>#</th>
              <th>Employee</th>
              <th>
                <div class="input-activity float-left">Activity</div>
                <div class="input-regular-hours float-left">Hours</div>
                <div class="header-overtime float-left">Overtime</div>
                <div class="float-left">Transfer</div>
                <div class="float-left input-zone"> Workstation</div>
                <div class="float-left"></div>
              </th>
              <th class="table-size">Comments</th>
            </tr>
          </thead>
          <tbody id="entries">
            <div 
            id="entries-prototype"
            data-prototype="{{ _self.entryCollection(form.input_entries.vars.prototype)|e }}">
              {% for entry in form.input_entries %}
                {{ _self.entryCollection(entry) }}
              {% endfor %}
            </div>
          </tbody>
        </table>
      </div>
    </div>
 
    <div class="form-group">
      {{ form_widget(form.save, {'attr': {'class': 'btn btn-success pull-right'}}) }}
    </div>
    
    {{ form_rest(form) }}
    {{ form_end(form) }}
  </div>
{% endblock %}

{% block javascripts %}
  {{ parent() }}

  <script type="text/javascript">
    //global variable containing details of all employees
    var employeesData = [];
    var singleData = [];
    {% for employee in listEmployees %}
    singleData = [{{employee.id|json_encode|raw}}, {{employee.name|json_encode|raw}}, {{employee.sesa|json_encode|raw}},{{employee.defaultTeam.id|json_encode|raw}},{{employee.defaultShift.id|json_encode|raw}},{{employee.defaultActivity.id|json_encode|raw}}];
    employeesData.push(singleData);
    {% endfor %}
  </script>
  {% javascripts '@SEInputBundle/Resources/public/js/*' %}
    <script type="text/javascript" src="{{ asset_url }}"></script>
  {% endjavascripts %}
{% endblock %}